'use client';

import React from 'react';

/**
 * ApiProvider
 * Standard provider for API operations.
 */
export default function ClerkApiProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
